Modifiche:
    1) è stata cambiata la funzione che calcola saving che ora esiste in due versioni: discreta e continua (modifico di [-1,+1] il saving in base alla distanza dall 'albergo di riferimento)
        occorrre rilanciare una volta enrich_DB() che le calcola entrambe (NB: dopo il saving range è solo 0-30)
    2) Sia DB_server che test2 la usano (DB_server ora è velocissimo)
    3) I casi di test sono letti dal file testCases.txt sia da test2 che da DB_server: vanno modificati solo lì . esistono due campi per la numerosità della ground true tuples (discreta) e tuplesf (continua)), leggermente differenti
    4) Ho risolto il problema di approssimazione sulle box modificando il codice di Marius. Ora i punti che cadono sui bordi sono inclusi nel box
    5) Il file DB_server_config.txt permette di selezionare la funzione discreta o continua 'floatSaving':False --> discreta, 'floatSaving':True --> continua
        è usato sia da DB_server che da test2 che produce i file di log con gli stessi nomi ma aggiunge la label FLOAT prima di M nel caso di funzione continua
        credo che la funzione continua scateni query + complesse, ma non ho verificato
    
